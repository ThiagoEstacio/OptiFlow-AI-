Generic single-database configuration with async support for OptiFlow AI.

To create a new migration:
    alembic revision --autogenerate -m "description of changes"

To apply migrations:
    alembic upgrade head

To rollback:
    alembic downgrade -1

To see migration history:
    alembic history

To see current version:
    alembic current
